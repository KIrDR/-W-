datasource db {
  provider = "sqlite"
  url      = "file:./db.db?case=insensitive?"
}

generator client {
  provider = "prisma-client-js"
}

model USER {
  ID       Int      @id @default(autoincrement())
  USERNAME String   @unique
  PASSWORD String
}
